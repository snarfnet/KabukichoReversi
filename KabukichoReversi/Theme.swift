import SwiftUI

enum KTheme {
    static let bg = Color(red: 0.04, green: 0.02, blue: 0.08)
    static let street = Color(red: 0.08, green: 0.04, blue: 0.12)
    static let neon = Color(red: 1.0, green: 0.08, blue: 0.58)
    static let neonCyan = Color(red: 0.10, green: 0.95, blue: 0.90)
    static let neonYellow = Color(red: 1.0, green: 0.92, blue: 0.15)
    static let text = Color(red: 0.96, green: 0.93, blue: 0.96)
    static let sub = Color(red: 0.65, green: 0.55, blue: 0.70)
    static let panel = Color(red: 0.10, green: 0.06, blue: 0.14).opacity(0.92)
    static let card = Color.white.opacity(0.07)
    static let line = Color.white.opacity(0.10)
    static let board = Color(red: 0.06, green: 0.03, blue: 0.10)
    static let boardLine = Color.white.opacity(0.15)
    static let cellEmpty = Color.white.opacity(0.04)
    static let cellValid = Color.white.opacity(0.12)
}
